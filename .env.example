# Server
PORT=3000
NODE_ENV=development

# Authentication (REQUIRED) - use a long, random, unique value in production
JWT_SECRET=change-this-to-a-strong-random-secret
JWT_EXPIRES_IN=7d

# CORS - comma-separated list of allowed origins, or "*" to allow all
CORS_ORIGIN=*
